document.addEventListener('DOMContentLoaded', () => {
    const tabs = document.querySelectorAll('.tab-label');
    tabs.forEach(tab => {
        tab.addEventListener('click', () => {
            // Logic to show/hide contents
        });
    });
});
